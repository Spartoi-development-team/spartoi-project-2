Usage: scripts/install_omoc_noninteractive.sh

This script attempts to install Oh My OpenCode (OMOC) components in a
non-interactive way suitable for CI. It sets provider flags to prefer
github-copilot and disable other providers to avoid interactive prompts.
